# yaml-language-server: $schema=../../jsonschema/config.json

# note that this base image has no CMD so the result image
# requires an arg /app/main.bundle.js to start
base: gcr.io/distroless/nodejs24-debian12:nonroot@sha256:20a51c926c0bb68a9b1f7059c81516da002655f8a896a2cb7bc56b56974782b3
layers:
- localDir:
    path: target
    containerPath: /app
    ignore:
    - meta.json
    - spdx.json
    - main.bundle.js.map
tag: "{{.IMAGE}}"
